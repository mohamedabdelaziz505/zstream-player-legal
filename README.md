# ZStream Player legal pages

Public privacy and terms pages for the ZStream Player Android app.

After enabling GitHub Pages for the `main` branch, use:

- `/privacy-policy.html` as the Play Console privacy-policy URL.
- `/terms-of-use.html` as the terms URL if requested.
